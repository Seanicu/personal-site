# 个人博客网站技术方案

## 📋 方案概述

基于 **Astro 5 + Tailwind CSS** 构建的现代化个人博客网站,采用静态站点生成(SSG)技术,实现高性能、简洁架构和优秀的 SEO 表现。支持中英文双语切换,内容通过 Markdown 文件管理,一键部署到 Vercel。

**核心价值**:零 JavaScript 运行时、极致性能、简单维护、优秀的开发体验。

**版本说明**: 使用 Astro 5.x(当前 5.17+),相比 4.x 版本提供更快的构建速度、Server Islands 功能支持和更好的开发体验。

---

## 🎯 技术选型

### 核心框架

| 技术 | 版本 | 用途 | 优势 |
|------|------|------|------|
| **Astro** | 5.x | 静态站点生成器 | ⚡ 零 JS 默认输出<br>📦 内置 Content Collections<br>🔍 优秀的 SEO<br>🌐 原生 i18n 支持<br>🏝️ Server Islands 支持 |
| **Tailwind CSS** | 3.x / 4.x | 原子化 CSS 框架 | 🎨 快速开发<br>📱 响应式设计<br>⚙️ 高度可定制 |
| **TypeScript** | 5.x | 类型系统 | ✅ 类型安全<br>🛠️ 更好的开发体验 |

### 内容管理

- **Content Collections**：Astro 内置的内容管理系统
- **Markdown + Frontmatter**：简单直观的内容编写方式
- **Zod Schema**：内容类型验证

### 部署平台

- **Vercel**：零配置部署，自动 HTTPS，全球 CDN

---

## 📁 项目结构

```
personal-site/
├── src/
│   ├── content/
│   │   ├── blog/
│   │   │   ├── zh/               # 中文博客文章
│   │   │   │   ├── post-1.md
│   │   │   │   └── post-2.md
│   │   │   └── en/               # 英文博客文章
│   │   │       ├── post-1.md
│   │   │       └── post-2.md
│   │   └── config.ts             # Content Collections 配置
│   │
│   ├── layouts/
│   │   ├── BaseLayout.astro      # 基础布局
│   │   └── BlogPostLayout.astro  # 博客文章布局
│   │
│   ├── components/
│   │   ├── Header.astro          # 网站头部
│   │   ├── Footer.astro          # 网站底部
│   │   ├── BlogCard.astro        # 博客卡片
│   │   └── LanguageSwitcher.astro # 语言切换器
│   │
│   ├── pages/
│   │   ├── index.astro           # 中文首页
│   │   ├── blog/
│   │   │   ├── index.astro       # 中文博客列表
│   │   │   └── [slug].astro      # 中文单篇文章
│   │   │
│   │   └── en/                   # 英文版本
│   │       ├── index.astro
│   │       └── blog/
│   │           ├── index.astro
│   │           └── [slug].astro
│   │
│   ├── i18n/
│   │   ├── ui.ts                 # UI 文本翻译
│   │   └── utils.ts              # i18n 工具函数
│   │
│   └── styles/
│       └── global.css            # 全局样式（Tailwind）
│
├── public/
│   ├── favicon.ico
│   └── robots.txt
│
├── astro.config.mjs              # Astro 配置
├── tailwind.config.mjs           # Tailwind 配置
├── tsconfig.json                 # TypeScript 配置
└── package.json
```

---

## 🌐 多语言架构设计

### 路由策略

采用 **URL 路径前缀** 策略：

- **中文版本**（默认）：`/`、`/blog`、`/blog/post-slug`
- **英文版本**：`/en`、`/en/blog`、`/en/blog/post-slug`

### 内容组织

```typescript
// src/content/config.ts
import { defineCollection, z } from 'astro:content';

const blogCollection = defineCollection({
  type: 'content',
  schema: z.object({
    title: z.string(),
    description: z.string(),
    date: z.date(),
    tags: z.array(z.string()).optional(),
    draft: z.boolean().default(false),
    lang: z.enum(['zh', 'en']),
    translationKey: z.string(), // 关联中英文文章
  }),
});

export const collections = {
  blog: blogCollection,
};
```

### UI 翻译

```typescript
// src/i18n/ui.ts
export const ui = {
  zh: {
    'nav.home': '首页',
    'nav.blog': '博客',
    'nav.about': '关于',
    'blog.readMore': '阅读更多',
    'blog.published': '发布于',
  },
  en: {
    'nav.home': 'Home',
    'nav.blog': 'Blog',
    'nav.about': 'About',
    'blog.readMore': 'Read More',
    'blog.published': 'Published on',
  },
} as const;
```

---

## 🎨 设计系统

### 视觉风格

- **极简主义**：去除不必要的装饰，聚焦内容
- **现代排版**：使用系统字体栈，优秀的阅读体验
- **响应式设计**：移动优先，适配所有设备
- **暗色模式**（可选）：支持亮/暗主题切换

### Tailwind 配置示例

```javascript
// tailwind.config.mjs
export default {
  content: ['./src/**/*.{astro,html,js,jsx,md,mdx,svelte,ts,tsx,vue}'],
  theme: {
    extend: {
      colors: {
        primary: {
          50: '#f0f9ff',
          500: '#3b82f6',
          600: '#2563eb',
          700: '#1d4ed8',
        },
      },
      typography: {
        DEFAULT: {
          css: {
            maxWidth: '65ch',
            color: '#334155',
            a: {
              color: '#3b82f6',
              '&:hover': {
                color: '#2563eb',
              },
            },
          },
        },
      },
    },
  },
  plugins: [
    require('@tailwindcss/typography'),
  ],
};
```

### 组件设计原则

- **原子化组件**：可复用的小组件
- **无障碍访问**：遵循 ARIA 标准
- **语义化 HTML**：使用正确的 HTML 标签

---

## ⚡ 性能优化策略

### Astro 5.x 原生优化

1. **零 JavaScript 默认输出**
   - 纯静态 HTML/CSS 页面
   - 只在需要交互时添加 JS

2. **图片优化**
   - 使用 Astro 的 `<Image />` 组件
   - 自动 WebP/AVIF 转换、懒加载、响应式图片
   - 改进的图片处理性能

3. **CSS 优化**
   - Tailwind 自动删除未使用的样式
   - 内联关键 CSS

4. **Server Islands(可选)**
   - 延迟加载动态内容
   - 不阻塞页面初始渲染

### 构建优化

```javascript
// astro.config.mjs
export default defineConfig({
  output: 'static',
  compressHTML: true,
  build: {
    inlineStylesheets: 'auto',
  },
});
```

### CDN 加速

- Vercel 自动提供全球 CDN
- 边缘缓存，极速访问

---

## 🔍 SEO 优化方案

### 1. 元数据管理

```astro
---
// src/layouts/BaseLayout.astro
const { title, description, ogImage, lang = 'zh' } = Astro.props;
const canonicalURL = new URL(Astro.url.pathname, Astro.site);
---

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  
  <!-- 基础 SEO -->
  <title>{title}</title>
  <meta name="description" content={description}>
  <link rel="canonical" href={canonicalURL}>
  
  <!-- Open Graph -->
  <meta property="og:title" content={title}>
  <meta property="og:description" content={description}>
  <meta property="og:image" content={ogImage}>
  <meta property="og:type" content="website">
  <meta property="og:url" content={canonicalURL}>
  
  <!-- Twitter Card -->
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content={title}>
  <meta name="twitter:description" content={description}>
  <meta name="twitter:image" content={ogImage}>
  
  <!-- 多语言 SEO -->
  <link rel="alternate" hreflang="zh" href={`${Astro.site}`}>
  <link rel="alternate" hreflang="en" href={`${Astro.site}en/`}>
  <link rel="alternate" hreflang="x-default" href={`${Astro.site}`}>
</head>
```

### 2. 结构化数据

为博客文章添加 JSON-LD：

```javascript
{
  "@context": "https://schema.org",
  "@type": "BlogPosting",
  "headline": "文章标题",
  "datePublished": "2026-02-10",
  "author": {
    "@type": "Person",
    "name": "作者名"
  }
}
```

### 3. Sitemap 与 RSS

```javascript
// astro.config.mjs
import sitemap from '@astrojs/sitemap';

export default defineConfig({
  site: 'https://yourdomain.com',
  integrations: [sitemap()],
});
```

### 4. 语义化 HTML

- 使用 `<article>`、`<header>`、`<nav>` 等语义化标签
- 正确的标题层级（h1 → h2 → h3）
- 图片添加 `alt` 属性

---

## 📝 内容工作流

### 1. 中文文章创建

```bash
# 创建新文章
src/content/blog/zh/my-new-post.md
```

```markdown
---
title: '我的新文章'
description: '这是一篇关于...的文章'
date: 2026-02-10
tags: ['技术', '博客']
lang: 'zh'
translationKey: 'my-new-post'
---

文章内容...
```

### 2. AI 翻译为英文

使用 AI 工具（ChatGPT、Claude 等）翻译文章：

```bash
# 创建英文版本
src/content/blog/en/my-new-post.md
```

```markdown
---
title: 'My New Post'
description: 'This is an article about...'
date: 2026-02-10
tags: ['tech', 'blog']
lang: 'en'
translationKey: 'my-new-post'
---

Article content...
```

### 3. 自动关联

通过 `translationKey` 字段，系统自动关联中英文版本，在文章页面显示语言切换按钮。

---

## 🚀 部署流程

### Vercel 部署配置

1. **连接 Git 仓库**
   - 推送代码到 GitHub/GitLab
   - Vercel 自动检测 Astro 项目

2. **构建配置**
   ```json
   {
     "buildCommand": "npm run build",
     "outputDirectory": "dist",
     "framework": "astro"
   }
   ```

3. **环境变量**（如需）
   - `SITE_URL`: 网站 URL
   - 其他 API 密钥

4. **自动部署**
   - Git push → 自动构建 → 自动发布
   - 预览部署支持

### 自定义域名

```
yourdomain.com    → 中文版
yourdomain.com/en → 英文版
```

---

## 🛠️ 开发命令

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览构建结果
npm run preview

# 类型检查
npm run check
```

---

## 📦 核心依赖清单

```json
{
  "dependencies": {
    "astro": "^5.17.0",
    "@astrojs/tailwind": "^5.x",
    "tailwindcss": "^3.x",
    "@tailwindcss/typography": "^0.5.x"
  },
  "devDependencies": {
    "@astrojs/sitemap": "^3.x",
    "typescript": "^5.x"
  }
}
```

---

## ✅ 方案优势总结

### 1. 极简架构
- ✅ 单一框架（Astro），无复杂配置
- ✅ 文件系统路由，无需额外路由库
- ✅ 零后端，纯静态托管

### 2. 出色性能
- ⚡ Lighthouse 100 分性能指标
- 🚀 首屏加载 < 1s
- 📦 打包体积极小（~50KB 初始加载）

### 3. 开发体验
- 🔥 热模块替换（HMR）
- 🎯 TypeScript 类型提示
- 📝 Markdown 直接写作

### 4. SEO 友好
- 🔍 完整的元数据控制
- 🗺️ 自动生成 sitemap
- 🌐 多语言 SEO 支持

### 5. 零成本运营
- 💰 Vercel 免费套餐足够使用
- ⚡ 自动 HTTPS + CDN
- 🔒 静态站点，无安全隐患

---

## 🎯 实施步骤方案

### 开发模式建议

**采用：垂直切片开发 + 渐进式增强**

**原因：**
1. **不推荐传统 TDD** - 静态网站 UI 组件难以编写有意义的单元测试
2. **垂直切片开发** - 每个阶段完成一个完整的功能切片（从配置到 UI），可独立验证
3. **渐进式增强** - 从最简单的中文版本开始，逐步添加多语言、SEO 等功能
4. **端到端验证** - 每个阶段通过浏览器手动验证 + Lighthouse 性能检测

**验证策略：**
- ✅ 每个阶段使用 `npm run dev` 浏览器验证
- ✅ 使用 `npm run build` 确保无构建错误
- ✅ 使用 Chrome DevTools Lighthouse 检测性能
- ✅ 使用 `astro check` 进行类型检查

---

### 阶段 0：环境准备（预计 20 分钟）

**目标：** 创建项目骨架，配置开发环境

**任务清单：**
```bash
1. 初始化 Astro 项目
   npm create astro@latest . -- --template minimal --yes

2. 安装依赖
   npm install @astrojs/tailwind tailwindcss @tailwindcss/typography

3. 安装开发依赖
   npm install -D @astrojs/sitemap

4. 初始化 Tailwind
   npx tailwindcss init
```

**需要创建的配置文件：**
- `astro.config.mjs` - Astro 配置
- `tailwind.config.mjs` - Tailwind 配置
- `tsconfig.json` - TypeScript 配置（通常自动生成）

**验收标准：**
- [ ] `npm run dev` 可以启动开发服务器
- [ ] 访问 `http://localhost:4321` 可以看到默认页面
- [ ] 无配置错误

**验证命令：**
```bash
npm run dev
# 浏览器打开 http://localhost:4321
```

---

### 阶段 1：核心架构搭建（预计 1 小时）

**目标：** 搭建项目目录结构，创建基础布局和样式系统

**任务清单：**

**1.1 创建目录结构**
```
src/
├── content/
│   ├── blog/
│   │   └── zh/
│   └── config.ts
├── layouts/
│   └── BaseLayout.astro
├── components/
│   ├── Header.astro
│   └── Footer.astro
├── pages/
│   └── index.astro
└── styles/
    └── global.css
```

**1.2 配置 Content Collections**
- 创建 `src/content/config.ts`
- 定义博客文章 schema（title, description, date, tags, lang, translationKey）

**1.3 创建全局样式**
- 创建 `src/styles/global.css`
- 引入 Tailwind directives
- 定义基础排版样式

**1.4 创建基础布局**
- 创建 `src/layouts/BaseLayout.astro`
- 包含 HTML 基础结构、meta 标签、SEO 基础配置
- 引入全局样式

**1.5 创建基础组件**
- `Header.astro` - 包含网站标题和导航
- `Footer.astro` - 包含版权信息

**验收标准：**
- [ ] 目录结构完整
- [ ] Content Collections 配置正确，`astro check` 无错误
- [ ] BaseLayout 可以正常引用
- [ ] Tailwind 样式生效
- [ ] 首页显示基础布局（Header + Content + Footer）

**验证方法：**
```bash
# 1. 类型检查
npm run astro check

# 2. 构建检查
npm run build

# 3. 开发服务器验证
npm run dev
# 访问 http://localhost:4321，查看基础布局
```

**预期效果：**
- 能看到简单的页面框架（顶部导航、内容区域、底部）
- Tailwind 类名生效（如 `text-center`、`p-4`）

---

### 阶段 2：中文版本 MVP（预计 2-3 小时）

**目标：** 完成中文博客的核心功能：首页、博客列表、单篇文章展示

**任务清单：**

**2.1 创建示例博客文章**
- 创建 `src/content/blog/zh/post-1.md`
- 创建 `src/content/blog/zh/post-2.md`
- 创建 `src/content/blog/zh/post-3.md`
- 包含完整的 frontmatter（title, description, date, tags, lang: 'zh', translationKey）

**2.2 创建首页 (`src/pages/index.astro`)**
- 展示网站标题和简介
- 显示最新 3 篇博客文章摘要
- 使用 `getCollection('blog')` 获取文章列表
- 过滤 `lang: 'zh'` 的文章
- 按日期倒序排列

**2.3 创建博客列表页 (`src/pages/blog/index.astro`)**
- 显示所有中文博客文章
- 每篇文章显示：标题、描述、发布日期、标签
- 创建 `BlogCard.astro` 组件用于展示文章卡片

**2.4 创建单篇文章页面 (`src/pages/blog/[slug].astro`)**
- 使用 `getStaticPaths()` 生成所有文章路由
- 创建 `BlogPostLayout.astro` 布局
- 渲染文章内容（Markdown）
- 显示文章元信息（发布日期、标签）
- 使用 `@tailwindcss/typography` 插件美化文章排版

**2.5 完善导航**
- 更新 `Header.astro`
- 添加"首页"和"博客"链接

**验收标准：**
- [ ] 首页显示最新 3 篇文章
- [ ] 点击文章标题可以跳转到详情页
- [ ] 博客列表页显示所有文章
- [ ] 单篇文章页面正确渲染 Markdown 内容
- [ ] 排版美观、响应式设计生效
- [ ] 导航链接正常工作

**验证方法：**
```bash
# 1. 开发模式查看
npm run dev

# 2. 测试路由
访问 http://localhost:4321/
访问 http://localhost:4321/blog
访问 http://localhost:4321/blog/post-1

# 3. 构建测试
npm run build
npm run preview

# 4. 响应式测试
使用 Chrome DevTools 切换设备尺寸
```

**预期效果：**
- ✅ 可以浏览完整的中文博客网站
- ✅ 所有文章可以正常访问
- ✅ 移动端和桌面端布局正常

---

### 阶段 3：多语言支持（预计 2-3 小时）

**目标：** 添加英文版本，实现中英文切换

**任务清单：**

**3.1 创建 i18n 配置**
- 创建 `src/i18n/ui.ts` - UI 文本翻译字典
- 创建 `src/i18n/utils.ts` - i18n 工具函数（getLangFromUrl, useTranslations）

**3.2 创建英文博客文章**
- 创建 `src/content/blog/en/` 目录
- 创建对应的英文文章，使用相同的 `translationKey`
- 示例：`post-1.md` (en) 对应 `post-1.md` (zh)

**3.3 创建英文版本页面**
```
src/pages/en/
├── index.astro
└── blog/
    ├── index.astro
    └── [slug].astro
```
- 复制中文页面逻辑
- 修改为过滤 `lang: 'en'` 的文章
- 使用英文 UI 翻译

**3.4 创建语言切换器**
- 创建 `src/components/LanguageSwitcher.astro`
- 检测当前页面语言
- 提供切换到另一语言的链接
- 使用 `translationKey` 关联对应文章

**3.5 更新布局和组件**
- 更新 `BaseLayout.astro` - 添加 `lang` 属性
- 更新 `Header.astro` - 集成语言切换器，使用 i18n 翻译导航文本
- 更新 `Footer.astro` - 使用 i18n 翻译

**验收标准：**
- [ ] 中文版本：`/`、`/blog`、`/blog/post-1`
- [ ] 英文版本：`/en`、`/en/blog`、`/en/blog/post-1`
- [ ] 语言切换器在所有页面正常工作
- [ ] 切换语言后，对应文章页面自动跳转到翻译版本
- [ ] UI 文本（导航、按钮等）正确翻译
- [ ] HTML `lang` 属性正确

**验证方法：**
```bash
# 1. 开发模式
npm run dev

# 2. 测试所有路由
访问 http://localhost:4321/ （中文）
访问 http://localhost:4321/en （英文）
点击语言切换器，验证 URL 变化

# 3. 检查 HTML lang 属性
右键 -> 检查元素，查看 <html lang="zh"> 或 <html lang="en">

# 4. 验证文章关联
从中文文章页点击切换语言，应该跳转到对应的英文文章
```

**预期效果：**
- ✅ 中英文版本独立访问
- ✅ 语言切换流畅无错误
- ✅ UI 完全本地化

---

### 阶段 4：SEO 优化与元数据（预计 1-2 小时）

**目标：** 完善 SEO 配置，添加 sitemap、robots.txt、meta 标签

**任务清单：**

**4.1 完善 BaseLayout SEO 配置**
- 添加所有必要的 meta 标签
  - description, keywords
  - Open Graph (og:title, og:description, og:image, og:url)
  - Twitter Card
- 添加 canonical URL
- 添加 hreflang 标签（多语言 SEO）

**4.2 为博客文章添加结构化数据**
- 在 `BlogPostLayout.astro` 中添加 JSON-LD
- 使用 BlogPosting schema
- 包含 headline, datePublished, author, image

**4.3 配置 Sitemap**
- 更新 `astro.config.mjs`
- 添加 `@astrojs/sitemap` 集成
- 配置 `site` URL

**4.4 创建 robots.txt**
- 创建 `public/robots.txt`
- 允许所有爬虫
- 指向 sitemap

**4.5 优化 OG Image**
- 创建默认 OG 图片（1200x630px）
- 放置在 `public/og-image.jpg`
- 在 meta 标签中引用

**验收标准：**
- [ ] 每个页面有完整的 meta 标签
- [ ] 博客文章包含 JSON-LD 结构化数据
- [ ] 构建后生成 `dist/sitemap-*.xml`
- [ ] `robots.txt` 正确配置
- [ ] 使用 [Meta Tags 预览工具](https://metatags.io/) 验证 OG 标签

**验证方法：**
```bash
# 1. 构建项目
npm run build

# 2. 检查生成的文件
ls dist/sitemap-*.xml
cat dist/robots.txt

# 3. 预览模式验证
npm run preview

# 4. 使用浏览器开发工具
右键 -> 检查 -> Elements -> <head>
查看所有 meta 标签

# 5. 在线工具验证
访问任一页面，复制 URL
使用 https://metatags.io/ 验证 OG 预览
使用 https://search.google.com/test/rich-results 验证结构化数据
```

**预期效果：**
- ✅ 所有页面 SEO 友好
- ✅ 分享到社交媒体时显示正确的预览卡片
- ✅ 搜索引擎可以正确索引

---

### 阶段 5：视觉优化与响应式设计（预计 3-4 小时）

**目标：** 实现极简主义+文学感的视觉风格，采用瑞士排版风格的变体，打造独特的"大色块"交互体验

---

#### 📐 整体视觉风格定义

**设计哲学：**
- **极简主义**：去除不必要的装饰，聚焦内容本身
- **文学感**：模拟高质感纸质书籍的阅读体验
- **瑞士排版风格（Swiss Design）变体**：严格的网格系统、清晰的层级、大胆的留白

**核心视觉元素：**
1. **纸张感背景**：使用极浅的灰黄色或灰绿色背景（`#F5F5F0` 或 `#F2F2EB`），模拟艺术纸张质感
2. **非纯黑文字**：使用深炭灰色 `#1A1A1A`，提升阅读舒适度
3. **衬线体系统**：全站使用衬线体（Libre Baskerville + 思源宋体/Noto Serif SC），强化文学感
4. **瞬间触发的交互色块**：使用鲜艳的橙色 `#FF4500` 作为交互强调色，特点是 `transition: none`（瞬间触发，无过渡）

---

#### 📋 任务清单

**5.1 配置 Tailwind 主题与字体系统**
- 更新 `tailwind.config.mjs`，定义颜色系统（paper、ink、accent）
- 定义字阶系统（hero: 64px, post-title: 28px, body-lg: 20px 等）
- 配置衬线体 fontFamily（Libre Baskerville, Noto Serif SC）
- 定义大留白 spacing（72px, 104px, 120px）
- 配置 typography 插件，自定义链接 hover 效果（橙色背景+白色文字）
- 在 `global.css` 引入 Google Fonts 字体文件

**5.2 实现首页与博客列表页布局**
- 采用非对称分栏布局（左侧 20-30%，右侧 70-80%）
- 左侧显示栏目标签（"About this blog"、"Blog posts"）
- 右侧显示具体内容
- 更新 BlogCard 组件：hover 时整个区域背景瞬间变为橙色，文字变白
- 更新 Header 组件：导航链接 hover 时色块效果

**5.3 实现文章详情页布局**
- 左侧 20-30% 显示元数据（发布日期、阅读时间、标签），使用 sticky 定位
- 右侧 70-80% 显示文章正文，标题字号 48-64px，正文 18-20px
- 标题与正文间留白 120px+
- 实现首字下沉效果（可选）
- 图片可超宽显示（Bleed Layout）
- 引用块采用斜体、右缩进、无侧边线
- 底部文章导航采用大色块 hover 效果

**5.4 响应式适配**
- 移动端（< 768px）：分栏改为上下堆叠，标题字号调整
- 平板（768px - 1024px）：保持分栏，调整比例
- 桌面（> 1024px）：完整非对称分栏

**5.5 细节打磨**
- 更新 Footer 组件：极简设计，居中对齐
- 更新 LanguageSwitcher 组件：文字切换按钮，hover 色块效果
- 添加阅读进度条（可选）
- 添加返回顶部按钮（可选）

---

#### ✅ 验收标准

**视觉风格检查：**
- [ ] 全站使用纸张感背景色（`#F5F5F0` 或 `#F2F2EB`）
- [ ] 所有文字使用衬线体（Libre Baskerville + 思源宋体）
- [ ] 非纯黑文字色（`#1A1A1A`）
- [ ] 首页/博客列表页采用非对称分栏（左窄右宽）
- [ ] 文章详情页左侧元数据 sticky 定位生效

**交互效果检查：**
- [ ] 所有可点击元素 hover 时瞬间触发色块效果（无缓慢过渡）
- [ ] 色块颜色为橙色 `#FF4500`，文字变白色
- [ ] 导航链接、博客卡片、文章内链接、底部导航均有色块效果

**排版细节检查：**
- [ ] 文章标题字号为 48px-64px，加粗
- [ ] 正文字号 18-20px，行高 1.8-2.0
- [ ] 标题与正文间留白 120px+
- [ ] 文章首字下沉效果（可选）
- [ ] 引用块为斜体、右缩进、无侧边线

**响应式检查：**
- [ ] 移动端（375px）：分栏变为上下堆叠，布局正常
- [ ] 平板（768px）：左右分栏正常显示
- [ ] 桌面（1920px）：完整非对称分栏，视觉舒适

**性能与可访问性：**
- [ ] 字体文件加载正常（通过 Google Fonts）
- [ ] Lighthouse Accessibility 评分 > 90
- [ ] 无控制台错误或警告

---

#### 🧪 验证方法

```bash
# 1. 开发模式
npm run dev

# 2. 浏览器验证交互效果
# 访问首页，hover 所有链接，检查色块瞬间触发
# 访问文章页，hover 正文链接，检查橙色背景

# 3. 多设备响应式测试
Chrome DevTools -> 切换设备模式
- iPhone SE (375px)
- iPad (768px)  
- Desktop (1920px)

# 4. 字体加载检查
Chrome DevTools -> Network -> 筛选 Font
确认 Libre Baskerville 和 Noto Serif SC 加载成功

# 5. 色值验证
使用浏览器取色器工具（如 ColorZilla）
验证背景色、文字色、交互色是否符合设计规范

# 6. Lighthouse 评分
Chrome DevTools -> Lighthouse
- Performance: > 90
- Accessibility: > 90
- Best Practices: > 90
- SEO: 100
```

---

#### 🎯 预期效果

**视觉呈现：**
- ✅ 呈现出极简主义+文学感的独特风格
- ✅ 纸张感背景营造温暖、优雅的阅读氛围
- ✅ 衬线体字体系统提升内容的专业性和文学性
- ✅ 非对称分栏布局打破常规，增加视觉趣味

**交互体验：**
- ✅ "大色块"瞬间触发效果带来强烈的视觉反馈
- ✅ 橙色交互色形成鲜明对比，引导用户行为
- ✅ 无缓慢过渡动画，操作响应迅速、果断

**阅读体验：**
- ✅ 正文排版模拟纸质书籍，行高舒适、留白充足
- ✅ 首字下沉增加文学仪式感
- ✅ 图片可超宽显示，打破版心限制，增加视觉冲击力
- ✅ 左侧 sticky 元数据作为视觉锚点，便于定位

**技术质量：**
- ✅ 所有设备响应式布局流畅
- ✅ Lighthouse 高分通过
- ✅ 无 JavaScript 依赖（除可选的阅读进度条）

---

### 阶段 6：内容完善与部署（预计 1 小时）

**目标：** 添加实际内容，配置部署，上线网站

**任务清单：**

**6.1 内容准备**
- 撰写 3-5 篇真实博客文章（中文）
- 使用 AI 翻译为英文版本
- 准备个人简介页面（可选）
- 准备 About 页面（可选）

**6.2 Git 配置**
- 初始化 Git 仓库
- 创建 `.gitignore`（包含 node_modules、dist、.env）
- 提交所有代码
- 推送到 GitHub

**6.3 Vercel 部署**
```bash
# 1. 安装 Vercel CLI（可选）
npm i -g vercel

# 2. 或直接在 Vercel 网站导入 GitHub 仓库
```

- 登录 [Vercel](https://vercel.com)
- Import GitHub 仓库
- 自动检测 Astro 项目
- 配置环境变量（如需）
- 部署

**6.4 域名配置（可选）**
- 在 Vercel 添加自定义域名
- 配置 DNS（A 记录或 CNAME）

**6.5 最终验证**
```bash
# 1. 本地构建测试
npm run build
npm run preview

# 2. 部署后测试
访问生产环境 URL
测试所有页面
测试所有链接
验证 SEO meta 标签
```

**验收标准：**
- [ ] 所有真实内容已添加
- [ ] Git 仓库干净整洁
- [ ] Vercel 部署成功
- [ ] 生产环境运行正常
- [ ] 自定义域名配置成功（如适用）
- [ ] Lighthouse 生产环境评分 > 95

**验证方法：**
```bash
# 1. 生产环境 Lighthouse 测试
访问 https://your-domain.com
Chrome DevTools -> Lighthouse -> 生成报告

# 2. 跨浏览器测试
Chrome、Firefox、Safari 各测试一次

# 3. SEO 检查
Google Search Console 提交 sitemap
检查索引状态
```

**预期效果：**
- ✅ 网站成功上线
- ✅ 性能优异
- ✅ SEO 配置正确

---

## 📋 实施清单总览

### 快速验证命令
```bash
# 开发服务器
npm run dev

# 类型检查
npm run astro check

# 构建测试
npm run build

# 预览构建结果
npm run preview

# Lighthouse 测试（生产环境）
# Chrome DevTools -> Lighthouse -> Generate report
```

### Agent 执行提示

**每个阶段开始前：**
1. 阅读阶段目标和任务清单
2. 检查依赖关系（是否依赖前一阶段）
3. 准备好需要的文件路径和内容结构

**每个阶段完成后：**
1. 执行验收标准中的所有检查项
2. 运行验证命令确保无错误
3. 在浏览器中手动验证效果
4. 提交阶段性代码到 Git（可选）

**常见问题处理：**
- 构建错误：检查 `astro.config.mjs` 配置
- 类型错误：运行 `npm run astro check`
- 样式不生效：检查 Tailwind 配置和 `global.css` 引入
- 路由 404：检查文件路径和 `getStaticPaths()`

---

## 🎯 总时间估算

| 阶段 | 预计时间 | 累计时间 |
|------|---------|---------|
| 阶段 0: 环境准备 | 20 分钟 | 20 分钟 |
| 阶段 1: 核心架构 | 1 小时 | 1.3 小时 |
| 阶段 2: 中文版本 MVP | 2-3 小时 | 3.3-4.3 小时 |
| 阶段 3: 多语言支持 | 2-3 小时 | 5.3-7.3 小时 |
| 阶段 4: SEO 优化 | 1-2 小时 | 6.3-9.3 小时 |
| 阶段 5: 视觉优化 | 3-4 小时 | 9.3-13.3 小时 |
| 阶段 6: 部署上线 | 1 小时 | 10.3-14.3 小时 |

**总计：10-14 小时完成完整项目**

---

## 💡 开发模式总结

**选择理由：**
- ✅ **垂直切片** - 每个阶段都是可独立运行和验证的完整功能
- ✅ **渐进式增强** - 从简单到复杂，降低风险
- ✅ **快速反馈** - 每个阶段都能在浏览器中看到效果
- ✅ **AI Agent 友好** - 明确的输入输出，清晰的验收标准

**不采用传统 TDD 的原因：**
- ❌ UI 组件难以写出有价值的单元测试
- ❌ 静态网站主要是展示逻辑，而非业务逻辑
- ❌ 测试成本高于收益

**替代验证策略：**
- ✅ 端到端手动验证（浏览器）
- ✅ Lighthouse 自动化性能测试
- ✅ TypeScript 类型检查
- ✅ 构建成功即验证基本正确性

**总计：10-14 小时完成开发**

---

## 📚 参考资源

### 核心文档
- [Astro 5.x 官方文档](https://docs.astro.build/)
- [Astro Server Islands 指南](https://docs.astro.build/en/guides/server-islands/)
- [Tailwind CSS 文档](https://tailwindcss.com/docs)
- [Vercel 部署指南](https://vercel.com/docs)

### 功能指南
- [Content Collections](https://docs.astro.build/en/guides/content-collections/)
- [国际化(i18n)](https://docs.astro.build/en/guides/internationalization/)
- [图片优化](https://docs.astro.build/en/guides/images/)
- [SEO 最佳实践](https://docs.astro.build/en/guides/seo/)

### 升级资源
- [Astro 5.0 升级指南](https://docs.astro.build/en/guides/upgrade-to/v5/)
- [从 4.x 迁移到 5.x](https://docs.astro.build/en/guides/upgrade-to/v5/)

---

## 🔄 后续扩展可能性

### 静态功能扩展
1. **评论系统**:集成 Giscus(基于 GitHub Discussions)
2. **搜索功能**:使用 Pagefind 实现静态搜索
3. **阅读统计**:集成 Google Analytics 或 Plausible
4. **RSS 订阅**:自动生成 RSS feed
5. **标签系统**:文章标签分类浏览
6. **暗色模式**:主题切换功能
7. **更多语言**:扩展到更多语言版本

### 使用 Server Islands 的动态功能(可选)
8. **实时浏览量统计**:使用 Server Islands 显示实时阅读数
9. **评论计数**:动态加载评论数量
10. **个性化推荐**:基于用户偏好的文章推荐
11. **实时搜索**:服务端搜索 API

**实现示例**:
```astro
---
// src/components/ViewCount.astro
const slug = Astro.props.slug;
const count = await fetchViewCount(slug);
---
<span server:defer>
  {count} views
</span>
```

---

## 📞 技术支持

如需协助实施此方案，可以按照上述步骤逐步进行。每个阶段都有明确的任务清单，可以独立完成和测试。

**建议开发顺序**：
1. 先完成中文版本的基本功能
2. 测试确认无误后再添加英文版本
3. 最后进行视觉优化和性能调优

祝开发顺利！🎉
